/**
 * @typedef ScaleListResponse
 * @property {integer} status
 * @property {Array.<Scale>} scales
 */

/**
 * @typedef ScaleResponse
 * @property {integer} status
 * @property {Scale.model} scale
 */

/**
 * @typedef ScaleCreateResponse
 * @property {integer} status
 * @property {Scale.model} createdScale
 */

/**
 * @typedef ScaleUpdateResponse
 * @property {integer} status
 * @property {Scale.model} updatedScale
 */

/**
 * @typedef ScaleDeleteResponse
 * @property {integer} status
 */
